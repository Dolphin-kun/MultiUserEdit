<UserControl x:Class="MultiUserEdit.Views.FilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:icons="clr-namespace:YukkuriMovieMaker.Resources.Icons;assembly=YukkuriMovieMaker.Plugin"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="送信・受信ファイル管理" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" />

        <Border Grid.Row="1" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1"
                CornerRadius="4" Padding="8" Margin="0,0,0,8"
                Visibility="{Binding HasActiveTransfers, Converter={StaticResource BoolToVisConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding TransferHeaderText}" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,6" />

                <ItemsControl ItemsSource="{Binding ActiveTransfers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Grid.Column="0" Padding="4,1" Margin="0,0,6,0" CornerRadius="3"
                                        BorderThickness="1" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
                                    <TextBlock Text="{Binding DirectionText}" FontSize="10" />
                                </Border>

                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding DisplayName}" FontSize="11"
                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding SizeText}" FontSize="10"
                                           Opacity="0.75" Margin="6,0,0,0" VerticalAlignment="Center" />

                                <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                                             Value="{Binding Progress, Mode=OneWay}" Maximum="100" Height="4" Margin="0,3,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <ToolBarTray Grid.Row="2"  Background="Transparent">
            <ToolBar>
                <Button Command="{Binding RefreshCommand}" Cursor="Hand" ToolTip="更新" ToolBar.OverflowMode="Never">
                    <Path Data="{DynamicResource {x:Static icons:GeometryKeys.Refresh}}"
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                          Width="14" Height="14" Stretch="Uniform" />
                </Button>

                <Button Command="{Binding OpenFolderCommand}" Cursor="Hand" ToolTip="保存先を開く" ToolBar.OverflowMode="Never">
                    <Path Data="{DynamicResource {x:Static icons:GeometryKeys.FolderOpen}}"
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                          Width="14" Height="14" Stretch="Uniform" />
                </Button>

                <Separator />

                <Button Command="{Binding CleanFolderCommand}" Cursor="Hand" ToolTip="全消去" ToolBar.OverflowMode="Never">
                    <Path Data="{DynamicResource {x:Static icons:GeometryKeys.DeleteSweep}}"
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                          Width="14" Height="14" Stretch="Uniform" />
                </Button>
            </ToolBar>
        </ToolBarTray>

        <ListView Grid.Row="3" ItemsSource="{Binding Files}" SelectionMode="Single" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="種別" Width="44">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DirectionText}" ToolTip="{Binding StatusText}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="ファイル名" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FileName}" ToolTip="{Binding FilePath}" TextTrimming="CharacterEllipsis" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="サイズ" DisplayMemberBinding="{Binding FormattedSize}" Width="75" />
                    <GridViewColumn Header="操作" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" Cursor="Hand" Padding="4,2" ToolTip="ファイルを削除"
                                        Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisConverter}}">
                                    <Path Data="{DynamicResource {x:Static icons:GeometryKeys.Delete}}"
                                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="12" Height="12" Stretch="Uniform" />
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
