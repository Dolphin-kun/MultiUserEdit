<UserControl x:Class="MultiUserEdit.Views.Controls.FileTransferBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="400">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid x:Name="RootGrid" Visibility="{Binding IsTransferring, Converter={StaticResource BoolToVisConverter}}">
        <Border x:Name="BannerBorder" 
                Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" 
                BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" 
                BorderThickness="1" 
                CornerRadius="4" 
                Margin="8,4" 
                Padding="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270" Color="Black" Opacity="0.25" />
            </Border.Effect>
            
            <StackPanel>
                <!-- ヘッダー (ファイル名、進捗率、トグル「v」ボタン) -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Data="M12 2A10 10 0 0 0 2 12A10 10 0 0 0 12 22A10 10 0 0 0 22 12A10 10 0 0 0 12 2M12 4A8 8 0 0 1 20 12A8 8 0 0 1 12 20A8 8 0 0 1 4 12A8 8 0 0 1 12 4M12 6V12L16.2 14.2L17 12.9L13.5 10.9V6H12Z"
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Foreground}" 
                              Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding TransferStatusText}" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- 進捗パーセント表示 -->
                    <TextBlock Grid.Column="1" Text="{Binding TransferProgress, StringFormat='{}{0:F0}%'}" FontSize="11" Opacity="0.8" Margin="8,0" VerticalAlignment="Center" />

                    <!-- 「v」折りたたみトグルボタン -->
                    <Button Grid.Column="2" Click="OnToggleClick" Cursor="Hand" Background="Transparent" BorderThickness="0" Padding="4,2">
                        <Path x:Name="ArrowPath" Data="M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z" 
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                              Width="10" Height="10" Stretch="Uniform" />
                    </Button>
                </Grid>

                <!-- 展開時のみ表示する詳細プログレスバー -->
                <StackPanel x:Name="DetailsPanel" Margin="0,6,0,0">
                    <ProgressBar Value="{Binding TransferProgress}" Maximum="100" Height="6" Margin="0,2,0,0" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
