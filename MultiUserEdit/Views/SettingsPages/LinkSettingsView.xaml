<UserControl x:Class="MultiUserEdit.Views.SettingsPages.LinkSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">
            <TextBlock Text="連携" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

            <Border BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" CornerRadius="4" Padding="12">
                <StackPanel>
                    <TextBlock Text="Webディープリンク (ymm4-multi-user-edit://)" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,6" />
                    <TextBlock Text="ブラウザからワンクリックでYMM4を開いて自動参加するためのプロトコル設定です。（起動時に自動登録されます）" FontSize="10" Opacity="0.65" TextWrapping="Wrap" Margin="0,0,0,10" />

                    <StackPanel Orientation="Horizontal" Margin="0,2,0,8">
                        <TextBlock Text="現在のステータス: " FontSize="12" Opacity="0.8" />
                        <TextBlock FontSize="12" FontWeight="SemiBold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsProtocolRegistered}" Value="True">
                                            <Setter Property="Text" Value="登録済み (有効)" />
                                            <Setter Property="Foreground" Value="#4CAF50" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsProtocolRegistered}" Value="False">
                                            <Setter Property="Text" Value="未登録" />
                                            <Setter Property="Foreground" Value="#FF9800" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding RegisterProtocolCommand}" Cursor="Hand" Padding="10,4" Margin="0,0,8,0" Content="再登録する">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsProtocolRegistered}" Value="False">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Command="{Binding UnregisterProtocolCommand}" Cursor="Hand" Padding="10,4" Content="登録を削除・解除">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsProtocolRegistered}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
