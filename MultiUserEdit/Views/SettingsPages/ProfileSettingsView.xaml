<UserControl x:Class="MultiUserEdit.Views.SettingsPages.ProfileSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">
            <TextBlock Text="プロフィール" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

            <Border BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1" CornerRadius="4" Padding="12">
                <StackPanel>
                    <TextBlock Text="表示名" FontSize="11" Opacity="0.75" Margin="0,0,0,3" />
                    <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                             PreviewMouseWheel="OnInputPreviewMouseWheel"
                             MaxLength="32" Height="28" Padding="6,4" FontSize="12" VerticalContentAlignment="Center" />

                    <TextBlock Text="自己紹介" FontSize="11" Opacity="0.75" Margin="0,12,0,3" />
                    <TextBox x:Name="DescriptionBox" Text="{Binding UserDescription, UpdateSourceTrigger=LostFocus}"
                             PreviewMouseWheel="OnInputPreviewMouseWheel"
                             Height="76" Padding="6,4" FontSize="12" TextWrapping="Wrap"
                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
                    <TextBlock x:Name="DescriptionHint" FontSize="10" Opacity="0.6" Margin="0,3,0,0" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
